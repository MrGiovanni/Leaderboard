# 将静态内容部署到 GitHub Pages 的简易工作流程
name: Deploy static content to Pages

on:
  # 仅在推送到默认分支时运行。
  push:
    branches: ['main']

  # 这个选项可以使你手动在 Action tab 页面触发工作流
  workflow_dispatch:

# 设置 GITHUB_TOKEN 的权限，以允许部署到 GitHub Pages。
permissions:
  contents: read
  pages: write
  id-token: write

# 允许一个并发的部署
concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  # 单次部署的工作描述
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      # 检出代码
      - name: Checkout repository
        uses: actions/checkout@v4  # 更新为最新版本

      # 设置 Node.js 环境
      - name: Set up Node.js
        uses: actions/setup-node@v4  # 更新为最新版本
        with:
          node-version: 18
          cache: 'npm'

      # 安装依赖项
      - name: Install dependencies
        run: npm install

      # 构建项目
      - name: Build project
        run: npm run build

      # 检查构建目录是否生成
      - name: Debug Build Output
        run: ls -R ./dist

      # 配置 GitHub Pages 环境
      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4  # 更新为最新版本

      # 上传构建产物
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v1  # 使用正确版本，不涉及 v3 问题
        with:
          # 指定构建文件路径
          path: './dist'

      # 部署到 GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2  # 更新为最新版本
